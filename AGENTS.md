# Context-garden operator handoff

Current state consolidated 2026-09-06 23:58 UTC. Replace obsolete claims rather than appending contradictory overrides. Verify live state before acting. Historical handoffs in context-garden/docs/operator-history are evidence, not current instructions.

## Start here

Read .claude/skills/garden-operate/SKILL.md for supported actions and context-garden/docs/incident-protocol.md for interrupted basic operation. The garden is /home/joshua/garden; product worktrees are /home/joshua/work/worktrees/CG-NNN. Use installed .venv/bin/garden for state-only actions. Never hand-edit task status or race the scheduler state writer. Fast-forward is INACTIVE; context-garden/docs/fast-forward.md is the owner-invoked maintenance procedure, not an active queue.

## Owner authority and priorities

Josh delegates routine decisions, task approvals, direct fixes, PR merges and process-gap filing. Do not repeatedly ask him to interpret scheduler states. Preserve work and distinguish process liveness from labels. No extra agents without explicit authorization. Keep tests serial and resource-bounded.

Workers internally self-review, repair their findings and recheck before completion. They report acceptance-criterion evidence, not a separate self-review report. This is in principles/00-index.md and verified in generated briefs. Reviewers directly fixing code remains deferred by the owner.

Phase 05 stabilization remains UNPROVEN: four productive unattended hours, ten representative completions, recovery exercises, real application journeys and named-project adoption. Operator repairs are interventions. Onboarding and both Now pages merged; do not resurrect their old queue. Phase 06 and PR221/222 remain frozen. New features belong there as drafts.

## Active incident and current containment

Web responsiveness incident reopened around 21:04 UTC after Now2 timed out amid overlapping suites and service memory.high pressure. Earlier closure at 19:13 was superseded by recurrence. Incident record: context-garden/docs/incidents/2026-09-06-web-responsiveness.md. Initial retro is complete; recurrence recovery observation and final retro addendum remain due. Follow the incident protocol before closing.

Josh explicitly requested concurrency FOUR after CI offload, superseding the earlier two-run trial and no-increase instruction. Live max_parallel=4 and resources.max_parallel=4 across work/reviews/checks/base probes; review_parallel remains1. Selected stabilization work uses up to four shared slots while ordinary admission stays PAUSED. garden.yaml keeps conservative defaults1; persisted live overrides govern admission. min_memory_available_mb1536/min_temp_free_mb1024 and OS caps are unchanged. Applied through product actions without restart. A full shared slot is admission saturation, not evidence of memory pressure.

Persistent garden-serve.service caps: CPUQuota=200%, CPUWeight=20, MemoryHigh=3GiB, MemoryMax=4GiB, MemorySwapMax=512MiB. Web and tests still share this resource budget. CLI launches share garden admission now, but OS budget isolation across launch paths is NOT established; prefer capped server POST actions. Manual heavy work uses a bounded systemd user service, disk temp /home/joshua/work/operator-test-tmp, and serial tests. Never start a second garden server.

/tmp is a 3GiB tmpfs; /home/joshua/work/tmp links to /tmp/garden-work, recreated by the worker-temp drop-in. Never delete active temp. The earlier timed resource sampler ended. The existing operator heartbeat checks every two minutes during the four-slot trial until 2026-09-06 23:55 UTC, then returns to five minutes if pressure remains absent. Inspect actual cgroup/process pressure, not cumulative counters alone.

## Installed build and verification

Installed pin58e13b99ddaf62b751b01771e5660039a421d46c (CG363 worker CI offload), verified branch/PR/main CI success. Main run34065153856 passed. Installed at a drained fresh tick boundary around22:56; bounded installer135.4MiB peak, read-only venv permissions restored and existing service restarted as PID2008482. Previous pin6dd3ae49 replaced. Includes CG360 archive consistency repair and CG337 attention repairs. No active worker/check was interrupted. Ordinary admission remains paused and caps unchanged.

Effective resource_status initially active0/limit1, available memory5987MiB/temp2208MiB. HTTP /, /now2, /inbox, /config returned200 in .25-.68s. With recovery work active, Now2 .876s and Windows browser Now2 -> Open run -> Board succeeded. Shared gate reported active1/limit1 and zero free slots. Service memory333MiB, swap0, no memory.high/max/OOM events at that observation. This is initial verification, not the required sustained loaded recovery proof or an unattended phase pass.

## Active recovery work and remaining gap

CG361 priority0: resource isolation is PR244, not merged or installed. Work230331 finished; review231837 requested changes for unbounded cgroup detection, missing real workload/journey evidence, a nonhermetic resource test, unrelated generated snapshot and missing UI evidence. Revise232501 finished and review234901 found inherited leases still allow concurrent nested validations. Controlled continuation20260906T235529Z-revise/PID2099834 is active with fresh output, focused on that remaining enforcement defect. Existing main CI helper is incorporated. Preserve snapshot/stash evidence and exclude unrelated generated changes from the PR. Prior214006 timeout remains honest history; do not duplicate dispatch.

CG-338 caps parent run records and owns descendants; it DOES NOT cap several pytest suites started inside one worker or reserve CPU/memory for the web service. CG-361 is implementing this enforcement gap, including effective cgroup sensing and actual loaded web/control journeys. Run admission lock is garden-scoped, not automatically every garden on the host. Keep ordinary dispatch paused while this controlled repair runs. Workers must not alter production services or run parallel full suites during implementation.

CG-354 focused-test/Git-fixture task is PR245, changes_requested after review234710. Code, cleanup and exact-head CI were verified; only benchmark evidence remains wrong (post-split baseline and unequal temp-directory measurement). An operator can rerun the small serial comparison correctly and update evidence; no new feature implementation is needed. Work231053/revise233657 finished; old PIDs are not live. CG-360 was directly implemented by the operator alongside the sole worker. PR242 merged44b9a312e7f56c73de3b012a67ce2cff65da2d51 at22:16 UTC; task is DONE. Final full CI1201passed/3skipped; focused ext4 tests33passed and tmpfs14passed, all local tests under100MiB. Worktree /home/joshua/work/worktrees/CG-360, branch codex/cg-360-restore-index. Evidence: docs/incidents/CG-360-validation.md. NOW INSTALLED through58e13b99: the earlier installation hold is cleared; no production history has been moved or deleted. No production history moved/deleted. CG-362 priority1/ready covers external/manual task completion overhead. If main check failures recur, inspect actual check evidence; the known ext4 restore failure may be relevant, but do not label all failures or killed probes as the same source defect.

CG-329 PR233 retains the operator safety hold until shared admission is verified under intended work; account for the remaining per-run test gap. GitHub reported it non-draft at23:21, so do not assume its old draft label still provides a merge hold. CG337 PR235 is merged and DONE. CG-358 PR239 recovery controls awaits current review/rebase; CG-323, CG-322 and CG-293/PR240 need evidence-based state reconciliation. Do not retry a task from stale observations: retry can cancel a just-started automatic run. Check live run and PR state immediately before mutation.

CG-326 preempted at21:04: worktree/transcript preserved, extra patch /home/joshua/work/operator-test-tmp/CG326-recurrence.patch, ready to resume later. CG-294 preempted17:51: headf129ed553e6fcc7733be06d87628166ebbe0d454, dirty edits/transcript preserved, patch CG294-web-outage-recovery.patch in the same directory; four escaped tests stopped. CG-338 retry race backup CG338-retry-race-2013.patch remains preserved. Do not blindly reset or discard these artifacts.

## Measured test costs and follow-up

Owner-requested serial same-suite A/B at c1f75cb: tmpfs163s/969MiB peak; ext4 222s/1523MiB peak with I/O stalls .19% versus11.47%. Negligible CPU throttling and no memory pressure in either isolated run. Memory includes cache/kernel/descendants, not just Python heap. One ordered pair does not prove concurrent incident causality; it gives no basis to move live temp to disk for speed or peak memory. Multiple ~1GiB suites against shared3GiB high/4GiB max is a strong capacity warning, not exact additive accounting. All experiment units finished.

Evidence and scripts: context-garden/docs/incidents/test-temp-ab/README.md and adjacent JSON/source. Raw logs/temp preserved under /home/joshua/work/operator-test-tmp/io-ab-20260906. Ext4 archive round-trip failure reproduced separately; CG-360 tracks it.

The existing operate-context-garden heartbeat runs every two minutes through23:55 UTC for the owner-authorized four-slot trial, then five minutes if pressure is absent. Watch all four controlled tasks and actual resources; do not duplicate launches. Verify shared admission and descendant accounting during intended workload; obtain three representative journey checks over ten minutes with loaded evidence before declaring incident recovery. Complete recurrence retro/prevention addendum; CG360 is installed and its earlier installation hold is cleared. Restore ordinary25-minute cadence only after recovery/follow-up, retaining unresolved safety limits as appropriate.

Owner authorized useful direct implementation alongside the earlier sole worker. With four scheduled tasks now active, avoid additional heavy operator work; keep any direct changes independent and tests serial, without starting another model agent. CG-360 is the first completed direct task.

At22:08 pytest1962007 was stalled after its Git push child exited128 but orphan fixture helpers1995748/1995749 retained stderr. Operator verified process identity/start times/cwd/pipes and terminated only those helpers in the disposable test_persona_without_sections_0 fixture. Pytest then finished425passed/1fixtureerror after694.80s. CG-361 agent and commits were preserved; no full-suite pass is established. Process evidence is operator-test-tmp/CG361-orphan-git-helpers.json; task notes and CG-354 record the incident. Diagnose original Git failure before another full retry. Verify fresh CG-361 output/processes; last observed commits included df9b4e7 with QA lease isolation.

CG-360 manual finish bookkeeping initially failed after an operator worktree move. Original path restored; all protected hashes match the dispatch snapshot. Only the resolved block for that exact manual run was archived (operator-test-tmp/CG360-resolved-git-block.txt); the clone is no longer blocked. Actual branch/PR recorded, normal ancestry-checked set-status done succeeded without force. Failed manual run220127 remains honest audit evidence, not successful automated work. CG-362 tracks a supported external completion workflow; avoid moving a worktree after take has snapshotted it.

At22:04 Now2 returned200/1.171s during worker test workload, service memory1.32GiB/swap0 and no memory.high/OOM. This adds recovery observation; recurrence closure and final retro addendum remain outstanding. That observation preceded the58e13b99 rollout described above; ordinary admission stays paused.

Read git log before pushing; commit only intended files. Many scheduler-owned task/spend/friction changes may be dirty. Codex operator spend remains unavailable until CG-336; never substitute Claude spend. Global resume is POST http://127.0.0.1:8765/resume with matching loopback Origin; CLI resume requires a task id. Routine pin updates require accounted drain; incident restarts follow the preservation procedure. No fast-forward drop-in or fixture server is active.

## Worker CI rollout, 2026-09-06 23:02 UTC

CG363 is DONE; PR243 merged58e13b99 and is installed. This product enables setup.worker_push=true and setup.test=python3 scripts/check_ci.py. Workers use focused local checks, commit, push only their assigned branch without force, wait for exact-final-commit CI, fix failures and report ordinary acceptance evidence. The scheduler still owns PRs and merging. GH_CONFIG_DIR is explicitly configured; HOME stays isolated. Existing branches must incorporate current main's workflow/helper before using it. Resource-specific fixture testing stays serial/bounded.

Final branch run34064850874 passed1224 tests/3skipped at ec0876b4e12984f2f9539d1afb9bcf9ea6d60bc4. Same-commit recheck reused that run; a successful old-head run was correctly rejected after checkout movement. PR and main CI passed.47 focused CI/brief tests and30 merge-gate tests passed under116MiB. Configured pre-PR checks retain local lint without repeating the full suite; the separate docs-product behavior is preserved. Full PR CI remains required, and opted-in products cannot automerge with no CI result.

Installed CG361 brief and real check commands verified. Post-restart /,Now2,Inbox,Config returned200 in1.790/.563/.615/.253s; Windows Now2 returned200/2.044s. Service142MiB,swap0,zero high/max/OOM events. Evidence: context-garden/docs/incidents/CG-363-validation.md. These idle rollout checks do not establish loaded incident recovery or unattended stabilization. Keep ordinary admission paused except for the four explicitly launched recovery/stabilization tasks. Trial monitoring, loaded recovery checks and recurrence retro remain due.

## Controlled four-slot trial, owner-authorized 2026-09-06

At23:21 verified live worker/shared limits4 and reviewer1. No service restart or OS cap change. At23:25:52 all four run supervisors were live with output less than31seconds old: CG354 work231053/PID2025473 (focused tests/Git-fixture cleanup), CG341 work232301/PID2044143 (mechanical evidence recorder/closure gate), CG361 revise232501/PID2045385 (resource-isolation review fixes), CG340 work232518/PID2046419 (independent adoption fixture). CG341/340 briefs scope independent work, remote full CI and bounded local checks. CG340 must distinguish repeatable fixture evidence from real named-project/maintainer adoption, which remains UNPROVEN without an authorized target. Dispatch timeouts were followed by verified PIDs; do not duplicate.

Four-run resource observation: memory.current3121643520 bytes, including anon468156416 and file2495832064; service swap0, no max/OOM events, memory PSI averages0, CPU PSIavg10 .05%, I/O PSIavg10 11.81% during startup. memory.high reached1879 events after cache-heavy startup (100 before further starts), with cumulative memory stall28209microseconds. Now2/Inbox/Config returned200 in .761/.748/.331s, host available5535MiB and temp2173MiB. This is not a no-high-event run or an incident closure. File cache dominates the current charge; inspect its reclaimability, anonymous/shared memory, pressure and counter growth rather than interpreting charged bytes alone as a leak.

Keep two-minute monitoring through23:55UTC. At memory above2.5GiB or a soft-limit crossing, inspect memory.stat, active tests, event deltas and user-path latency before further admission. If pressure persists/rises after startup, anon/shmem growth consumes headroom, temp free approaches1GiB, or pages repeatedly exceed2seconds/time out, reduce live worker/shared admission to1 through supported actions. Preserve/preempt lower-priority work only if pressure does not drain. Never increase OS caps to mask contention. No additional local full suites. Loaded recovery journeys and recurrence retro remain due; operator-directed trial is not unattended stabilization evidence.

At23:27-23:28 the four-slot trial remained responsive: I/O PSIavg10 settled to0, memory PSI remained0, service swap/max/OOM remained0, and memory.current was3118247936 bytes. memory.high2342 is a cumulative soft-limit/cache-reclaim count, not zero; track deltas. Windows first Now2 request2.557s, followed by Now2 .819s, Inbox .749s and Now2 .640s (all200). Continue monitoring; this does not establish sustained loaded incident recovery.

## Snapshot salvage recurrence and trial check, 23:35-23:38 UTC

CG354/PR245 review caught scheduler leftover commit dd7ff92224af1217853a48ab17e07117ca92628e adding roughly69000 unrelated snapshot lines after exact-head worker CI at c4e6ca3. CG361 had the same finding. CG341 completed intended code9122d7f3ff1da7ae833f28ac7154674e88740b6b with CI34066986116 success and explicitly restored its unrelated snapshot before finishing, entering the same risky path. CG359 is now priority0 for the existing salvage/provenance root cause, not a duplicate task. It was selected within the owner-authorized four-run cap: work20260906T233716Z-work/PID2060486, verified live and fresh. CG354 brief now says preserve unrelated snapshot in a named stash/recovery reference and leave it out of the dirty worktree at completion, preventing reintroduction while CG359 is pending. No recovery data was discarded and no production patch applied.

At23:37:51 live processes: CG361 revise232501/PID2045385, CG354 revise233657/PID2059397, CG359 work233716/PID2060486. CG340 work/check finished and PR246 review requested changes; inspect feedback before continuing. CG341 PR247 is in_review and eligible for the one-review slot. Finished check supervisors are not live workers. Preserve normal phase holds and global pause; controlled work/reviews continue with worker/shared4,review1.

Resources23:35: memory2.50GiB,high2342 unchanged since23:27,memory/CPU/IO PSIavg10 all0,swap/max/OOM0; Now2/Inbox/Config .604/.660/.261s and WindowsNow2 .780s. At23:37 after another startup: memory3125350400 bytes,anon326569984/file2645655552,high2622, memory PSI0,IO PSIavg10 6.14%,swap/max/OOM0; Now2 .652s/Inbox .691s. Additional cache/IO startup pressure still resolves without sustained memory PSI; keep two-minute monitoring until23:55 and check deltas before replenishing slots. No loaded control-journey incident closure or unattended stabilization claim.

CG364 is approved priority2, depends onCG354/CG361: remove demonstrated redundant/ineffective assertions and unnecessary fixture work, preserving distinct regression cases and measuring actual savings. This is queued only; no tests were removed by the operator audit.

At23:41 the three current workers had live PIDs; memory3177615360 bytes was mostlyfile2565791744/anon389193728,shmem140050432,high3415,cumulative memory stall42822microseconds, memory/IO PSIavg10 zero,swap/max/OOM0,Now2 .711s/Inbox .730s. Continue monitoring; no material pressure recurrence.

CG340/PR246 received a mechanical operator cleanup at23:43 with no active run: preserved snapshot6dc8fb0 under operator-test-tmp/snapshot-salvage-20260906T2343Z (JSON plus hashed backup), restored only docs/design/snapshot.json from base58e13b99 and pushed0484952b76a9fa0144ed08868e944dfa8d27b4eb. Verified its entire Git tree equals intended worker7df508f2f1706b6a2511389c74d5553f0e1b0fd3. PreviousCI evidence is not called a new-head pass; new CI pending, PR body updated. After current-head CI succeeds, update evidence and request review through supported actions; do not spend a worker revision removing the already-cleaned artifact. CG341/PR247 still follows ordinary review; do not alter its branch while a reviewer is active. This is a supervised intervention, not unattended evidence.

## Current review/deployment queue, 23:58 UTC

CG340/PR246 MERGED atf41b414ccf816a56d2c7dc17b85e300f4d13cb2d,23:57:34 after exact-head review approval, both head CI passes, permanent description rewrite and GitHub CLEAN. Let normal poll reconcile taskDONE. Repeatable compatibility PASS; real-user adoption remains UNPROVEN. Old pending feedback/needs-human survived description-only approval; CG362 records that operator workflow defect. Do not redispatchCG340.

CG341/PR247 is held changes_requested after operator removed only snapshot1c18bfd; cleanup426e0c82429a422c46a884a2389659ea31677768 has the complete intended9122d7f tree. Branch CI34067953339/PR CI34067955193 both passed at23:54. CG359/PR248 is similarly held before first review, cleanup2c889374a81d956b61971b496a149995c657a41f equals intended4a3565b tree; branchCI34068102211/PRCI34068103543 pending at23:55. Hashed backups and cleanup metadata for both are under operator-test-tmp/snapshot-salvage-20260906T2343Z. Prioritize CG359 review/merge/verified deployment so new completions stop suffering this defect. Read new-head CI, replace stale PR verification prose and respect reviewer1; triage-ready alone does not enqueue a new review or clear pending feedback. Verify no reviewer is live before explicit review action. Current main advanced withPR246; reconcile bases/CI before serial merges. Avoid fresh unrelated worker starts while the critical salvage fix is awaiting deployment.

At23:55:56 only CG361 revise235529/PID2099834 was live, with2-second output freshness. Worker/shared limits remain4,review1. memory2670649344 bytes,high3589 unchanged since23:47, memory/CPU/IO PSIavg10 all0,swap/max/OOM0; Now2 .603s/Inbox .651s,host available5811MiB/temp2163MiB. Initial23:47 Now2 2.127s fell to .638/.601s on repeat, so no sustained latency recurrence was observed. The timed two-minute trial has ended; existing operator automation now returns to five-minute monitoring. This is not incident closure: loaded control journeys, recurrence retro and actual resource-isolation enforcement remain outstanding. Installed pin still58e13b99 until live verification says otherwise.
