# Context-garden operator handoff

Current state consolidated 2026-09-07 01:04 UTC. Verify live state before acting. Replace obsolete state instead of appending contradictions. Prior detailed observations and recovery provenance are archived in context-garden/docs/operator-history/handoff-20260907T0052-before-consolidation.md; they are history, not current instructions.

## Start here and authority

Read .claude/skills/garden-operate/SKILL.md and context-garden/docs/incident-protocol.md. Garden: /home/joshua/garden; product worktrees: /home/joshua/work/worktrees/CG-NNN; shared product clone: /home/joshua/work/repos/context-garden. Use installed .venv/bin/garden for state-only actions. Never hand-edit task status or race the scheduler state writer.

Josh delegates routine decisions, approvals, direct fixes, PR merges and process-gap filing. Preserve unfinished work and distinguish live processes from labels. No extra model agents without explicit authorization. Scheduled workers have owner-authorized concurrency FOUR. Workers self-review internally, repair findings and recheck before completion; report normal acceptance evidence. Reviewers directly fixing code remains deferred.

Fast-forward is INACTIVE. Its historical ledger is context-garden/docs/fast-forward.md, not an active maintenance queue. Phase06 and PR221/222 remain frozen; new features belong there as drafts. CG329/PR233 retains its operator safety hold; its non-draft GitHub state is not permission to merge. Phase05 remains UNPROVEN: four productive unattended hours, ten representative completions, recovery exercises, actual application journeys and named-project adoption. Operator interventions do not count as unattended progress. Both Now pages and main Now CLI repair merged; do not resurrect old queues.

## Resource containment and current build

Ordinary admission remains PAUSED for the web/resource incident; selected recovery/stabilization work runs through explicit capped server actions. Live max_parallel=4 and resources.max_parallel=4, review_parallel=1; YAML defaults1 do not override persisted live settings. Memory available threshold1536MiB, temp1024MiB. Keep CPUQuota200%, CPUWeight20, MemoryHigh3GiB, MemoryMax4GiB, MemorySwapMax512MiB. Never start a second server or raise OS caps to mask contention.

Installed pin fc65880957788c5240abd8ef69281a639d4a876c includes CG359 snapshot salvage and CG340 onboarding fixture, plus earlier CG363 CI offload, CG360 archive consistency and CG337 attention repairs. Exact main CI34070336947 passed. Deployed at a verified drained fresh-tick boundary 00:48:50–00:48:55 UTC using bounded CPU200%/MemoryMax1GiB/no-swap installer (159MiB peak). Read-only venv permissions restored. Existing normal-watch garden-serve.service restarted as PID2146567; pause, limits and phase holds preserved. No active work was interrupted. Now2/Inbox/Config returned200 in .715/.718/.285s after deployment. This is initial rollout verification, not incident closure.

/tmp is3GiB tmpfs; /home/joshua/work/tmp links to /tmp/garden-work, recreated by the worker-temp drop-in. Never delete active temp/history/caches. Manual heavy work must use a separate bounded systemd user unit and disk temp /home/joshua/work/operator-test-tmp. Local tests stay focused, serial and bounded; full suites run on exact-final-commit GitHub CI using python3 scripts/check_ci.py. setup.worker_push=true permits assigned-branch normal pushes only; scheduler owns PR creation/merging. Never reuse old-head CI after branch changes.

## Actual active work and next actions

CG361/PR244: revise run20260907T005035Z-revise, PID2146780 verified live. Previous headb398cee passed CI but review002007 requested coherent authoritative per-user heavy-validation capacity: different gardens' limit1/limit2 values can exceed the claimed shared budget. Updated brief also fixes the whole-worker lease design: heavy budget1 must govern expensive supported setup/validation/check/probe execution, not serialize FOUR model sessions or remote CI waits. Keep separate aggregate execution cgroup limits, descendant ownership, cancellation and honest unsupported-command boundaries. CG365 is an existing discovered draft for the same host-policy issue; avoid duplicates or deferring a blocking guarantee. Incorporate current main and validate affected behavior. Worker may not change production services. CG361 is NOT merged/installed; production web and execution still share a cgroup until deployment establishes the separate boundary.

CG354/PR245 MERGED at332ee916c22fd7e0929bd1f36207d3e95351c2b0, 01:01:52 UTC. Current-head review005059 approved all criteria atf729f350, supplied permanent description rewrite (verified applied), branchCI34070444500/PRCI34070446774 passed and GitHub CLEAN. Owner-authorized merge cleared the obsolete pending-feedback stop without another worker. Verify normal DONE reconciliation; do not redispatch. Not installed yet; include with the next drained resource-repair rollout. Corrected benchmark in docs/validation/cg354 retains all34 historical retro cases: baseline34passed/6.29s/67084KiB RSS/14112KiB pytest temp versus focused6passed/.27s/54060KiB/0pytesttemp, same capped service peak153.8MiB. One ordered pair demonstrates focused-selection savings, not full-suite improvement.

CG341/PR247 held changes_requested awaiting current-head CI and first review. Following CG354 merge, integrated main332ee916 and pushed1b0894c11de51982eb654eea18d4e23aad5bd457 at01:02; branch-specific diff byte-identical. Previous6132f888 branch/PR CI passed; new-head CI pending, PR description refreshed. Request first review only with passed current-head CI and a free one-review slot. Implements mechanical stabilization gates, not proof that real soak/adoption is done.

CG361 latest progress01:03: worker committed7878424a524ccb01799f1280d9c9b3fbdc07746b after focused local validation/lint passed; exact-head branchCI34071471268/PRCI34071473874 pending. Evidence now demonstrates two model sessions concurrently while heavy validations peak1, and conflicting limit2 respects authoritative limit1. Worker supervisor2146780 remains live awaiting CI. Main advanced after its commit withCG354: after it finishes, reconcile against current main before final review/merge, preserving current work. Do not alter its worktree while worker/CI helper is active.

CG359/PR248 mergedfc658809 at00:36:15; taskDONE and now installed. Its current-head review approved all criteria; exact branch/PR/main CI passed. Named run-scoped salvage stashes preserve excluded dirty artifacts without adding them to PRs. Do not keep waiting for its deployment or redispatch it. CG340/PR246 mergedf41b414 and taskDONE; repeatable compatibility fixturePASS, real-user adoptionUNPROVEN.

CG364 readyP2 depends onCG354/361 for justified redundant-test/fixture pruning. Concrete audit in its brief; no tests deleted by operator audit. CG362 tracks manual completion/triage feedback defects AND avoidable idle recovery gaps. When a prerequisite passes, perform its next action in the same check-in; don't leave all slots empty while repeating stale waiting status.

## Review/state pitfalls and recovery evidence

Triage-ready makes a task eligible but does not enqueue review or clear pending feedback; it may immediately show a stale in_review stop. Explicit /review starts immediately and can bypass reviewer admission: verify the one-review slot is free first. Approved current-head reviews can retain obsolete pending feedback. Use supported actions and actual head/CI evidence; never fabricate approval or manually edit state. Owner-authorized manual merge is permitted after verified current approval, passed exact-head CI and GitHub CLEAN; normal poll reconcilesDONE. CG362 owns these gaps. A timed-out POST does not cancel action: inspect resulting run/PID/output before retrying.

Snapshot backups with hashes remain at /home/joshua/work/operator-test-tmp/snapshot-salvage-20260906T2343Z for CG340/341/359. Cleanup commits preserve original recovery data, no force pushes. CG326 preempted work/patch CG326-recurrence.patch, CG294 headf129ed553e6fcc7733be06d87628166ebbe0d454/patch CG294-web-outage-recovery.patch, and CG338-retry-race-2013.patch remain preserved in operator-test-tmp. Other old run/state reconciliation needs evidence, not blind retry. CG360 manual-run artifact details are in archived handoff and docs/incidents/CG-360-validation.md; original worktree path was restored and taskDONE. Never move a worktree after take snapshots its fence.

## Monitoring and incident completion

Existing operate-context-garden heartbeat every FIVE minutes, no extra monitor. Resource trial showed startup charge mostly file cache, brief I/O pressure and cumulative memory.high3589, with only44ms cumulative memory stalls, zero max/OOM and later PSI0. Before latest restart, available memory5.8GiB/load.08/service274MiB. Counters reset with new service cgroup: compare deltas, not old totals. Above2.5GiB or high crossings inspect anon/file/shmem, reclaimability, actual descendants and latency. Persistent/rising pressure, exhausted headroom, temp near1GiB or repeated pages>2seconds/timeouts requires supported live worker/shared reduction to1, preserve/preempt only if it does not drain. Do not relax caps.

Incident context-garden/docs/incidents/2026-09-06-web-responsiveness.md remains open. Required recovery: three actual Now/Inbox/control journeys over at least ten minutes, including intended workload, resource evidence, recurrence retro and prevention follow-up. Initial green route probes are not closure. Restore ordinary admission only with verified recovery; then restore normal25-minute duties when appropriate. Keep follow-up in automation until complete.

Serial same-suite tmpfs/ext4 evidence in context-garden/docs/incidents/test-temp-ab:163s/969MiB versus222s/1523MiB, I/O stalls .19% versus11.47%. Not a basis to move live temp to disk or claim proven original OOM causality. Overlapping suites, tmpfs and duplicated state manifests were measured contributors. Preserve history; CG360 archive repair is installed but no production history was moved/deleted.

Read git log before pushing and commit only intended files; scheduler-owned spend/friction/task files may be dirty. Codex operator spend unavailable untilCG336; never substitute Claude spend. Global resume is POST /resume with matching loopback Origin; CLI resume requires task id.

Latest01:01 resource observation: service630476800bytes, new-cgroup high/max/OOM0,memoryPSI0,Now2 .676s. Browser existing Now2 tab still held an old Not Found document from before restart; live route succeeds. No refreshed browser control journey was established this turn; do not count it as recovery evidence.
