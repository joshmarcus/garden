# v0.3.0rc1 — remote AWS worker foundation

This preview adds portable remote workers, reusable EC2 host lifecycle, and the managed worker bootstrap to the installed v0.2.0rc1 build. Work, setup, checks and reviews can execute on a separate host with leased staging refs, streamed results, host attribution, an exclusive host slot and measured memory/disk admission.

Included lineage: v0.2.0rc1/d24e677; CG345/PR295 squash8416ed13; CG216/PR221 squashd501ae76; CG346/PR300 operator-reviewed headad1ca77c. The two prerequisite PRs were merged under the owner's earlier direct-review authorization, documented in their PRs and the garden's remote-operator-repair-20260907.md. PR300 uses the current explicit fast-forward self-review authorization. All sources require exact-head CI and PR300 merge before publication.

Selection adaptations: omit unrelated maintenance-pause documentation and ghost-record cleanup context while applying the remote protocol to v0.2.0rc1, which does not contain those features. Existing remote lease expiry and terminal-review handling are preserved. Tests and actual HTTP lifecycle checks must pass on this assembled source.

Excluded: unrelated newer main changes, pending Inbox/performance/recovery PRs, model pools and enterprise work. No arbitrary-main deployment.

Known limitations: the historical real EC2 canary used a deterministic model harness and fake GitHub API; it was terminated and its enrollment retired. Corrected setup/lock/recovery behavior is demonstrated through real local TCP HTTP and separate managed-worker subprocesses. Production Codex/Git authentication and clean-image replay remain deployment prerequisites. Spot recovery and pool controls are subsequent work. Phase05 stabilization and incident follow-up remain open.

The AWS aggregate allocation remains $80, including retained resources. Provision only with a scoped identity, bounded host/runtime plan and independent cleanup. Budget reports are delayed.

Install the exact release source only at an accounted runtime boundary, restore venv protections, restart the existing service, verify version/source and affected interactions, then record deployment separately. Fast-forward keeps scheduling quiesced. Rollback to v0.2.0rc1/d24e677 at the same safe boundary; preserve task/run state and artifacts.

Validation completed: full GitHub CI34184425532 passed on exact source55b31a84;185focused tests and real managed-worker HTTP lifecycle passed with325.3MiB peak/no swap. Nine scripted application HTTP flows passed with117MiB peak/no swap. PR300 merged21e64a3c after both full head CI runs passed. Publication alone does not deploy.
