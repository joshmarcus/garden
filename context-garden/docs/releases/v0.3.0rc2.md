# v0.3.0rc2 — authenticated production worker bootstrap

This preview extends v0.3.0rc1 with CG419/PR301, the production Codex bootstrap. Codex0.153.4 is verified by official archive SHA256 and installed-version check. Dedicated model tokens are stored in a refreshable worker-owned cache, and a repository-scoped SSH key uses GitHub over443 with pinned host identity. The runtime retains no bootstrap model/Git credentials in its general configuration.

A worker's explicit host-owned Git environment paths are passed through setup and model execution; the controller's configured harness permission mode is preserved. The optional lifetime is bounded to six hours and enforced by an in-instance shutdown timer; an external termination deadline and actual resource inventory are still required. The existing unprivileged identity, metadata denial, disk temp, host lock and measured admission remain.

Lineage: installed55b31a84 plus operator-reviewed PR301/8de03e27. No unrelated main changes are included. Exact release CI and focused tests gate publication. A live production host/model run is a subsequent deployment verification, not implied by publishing this source.

The owner authorized an aggregate $80 AWS allocation. Use scoped provisioning, one bounded host at a time for this first rollout, dedicated credentials and fresh restricted enrollment. Keep the prior completed deterministic canary retired.

Install this exact release at a drained boundary with read-only venv protection restored and source/version/user journeys verified. Preserve fast-forward no-watch/pause. Roll back to v0.3.0rc1/55b31a84 without rewinding state or deleting evidence. Phase05 stabilization, Spot recovery and broader pool controls remain unproven.

Validation:49focused tests passed on the assembled source (156.5MiB/no swap); full releaseCI34186800461 passed. Both PR301 exact-head CI runs passed. Actual production host/model verification remains pending and is tracked separately by CG419.
