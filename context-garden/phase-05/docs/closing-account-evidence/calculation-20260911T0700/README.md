# Accounting snapshot, September 11 at 07:00:29 UTC

The exact accepted calculation source is `5d88157d1fd5d9a15fcd746a888d05e179c75543` in joshmarcus/context-garden. These are sanitized event/task/operator projections and results. Private raw event and operator snapshots remain outside this repository. Original September 10 results are preserved separately.

The recorded replay used a clean checkout of that exact source in a sibling `source` directory and verified the three calculation module hashes before matching every metric and the entire accepted cohort. To repeat in a disposable directory, copy these inputs and replay.py, create that exact checkout at `source`, and move the copied historical replay-verification.json aside so replay.py can write a new receipt. Original immutable receipt files must remain unchanged.

The earlier account called its old metrics implementation RC19. The preserved original result and original checkout identify `d640f92d41aaf5133254ddf927b8a473ce380740`; preparation.json records this attribution correction. No old inputs, results or measurements were changed.
