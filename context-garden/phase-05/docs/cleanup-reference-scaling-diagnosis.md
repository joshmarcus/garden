# Branch cleanup reference matching: observed RC20 scheduler stall

At 2026-09-11 05:25 UTC the first resumed RC20 scheduler pass had not returned. A read-only sampled stack of watch PID 1870478 shows `re.search` → `_has_run_reference` → `classify_branches` → `branch_cleanup_inventory` → `_branch_delete_recheck` → `delete_disposition` → `sweep_worker_branches`. At 05:37 the same process had accumulated more than 21 minutes of CPU time, with no child process. The private state snapshot is 13,250,012 bytes and the run inventory contains approximately 5,090 records. The complete private scheduler state must not be committed or published.

Installed RC20 source is c2c862a77eb04189dda0ad863a3510d5161d713d. CG619 correctly replaced prefix matching with literal boundary matching. The current implementation nevertheless performs a full state regex scan for every matching run ID, and a single-branch deletion recheck repeats recovery matching across all runs. CG599's bounded remote inventory repair is installed; this new observation is a CPU reference-matching problem, distinct from the earlier network scan failure. Preserve both historical failures.

A suitable bounded correction can build one fresh whole-token reference index per classification using the same Python Unicode word-or-hyphen alphabet as the existing boundaries. Native run IDs composed only of those characters can use exact membership. Arbitrary IDs containing literal punctuation need the existing escaped-boundary semantics or an equally proven fallback. Empty IDs, same-second suffixes, longer IDs, nested backup paths, artifact filenames, stash names, punctuation, Unicode, and regex metacharacters must retain their existing safety semantics. Do not reuse an index across mutable snapshots. Single-branch rechecks can restrict recovery matching to relevant branch provenance while still considering all active runs and base claims.

Acceptance requires differential correctness tests against the existing matching semantics, an operation-count or otherwise deterministic scaling regression, and an isolated synthetic benchmark at comparable state/run scale. Preserve all fresh PR, active-run, recovery, protected/default branch, stack base, worktree, source preservation, and compare-and-delete checks. Do not disable cleanup, change retention or delete private evidence. Validate exact source with normal CI and independent review. Product code must remain portable on Linux, macOS and Windows through WSL; report actual execution coverage without claiming untested platforms.

Root is performing a narrowly guarded operator recovery and temporarily reducing the existing supported cleanup batch limit from 20 to 1. Cleanup stays enabled. This operational mitigation is not the repair and must be restored after an accepted version containing the fix is activated. The existing Phase05 closing account must include the failure, recovery and actual outcome. No fleet, deadline, spending or release-policy change is authorized by this diagnosis.
