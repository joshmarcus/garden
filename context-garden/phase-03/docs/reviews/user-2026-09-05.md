# user review of context-garden/phase-03

**Persona:** user · **Score:** 6/10 · 2026-09-05T10:20:02+00:00

The phase delivered what I can see on a task page: a rebase before merge costs nothing, an approved diff is not re-reviewed, and the friction and retro documents are honest enough that I trust the tool more than I did. But 12 of the 30 merges were still done by hand, the merge-queue fix was itself hand-merged and never exercised live, config hot reload was in the definition of done and quietly did not happen with no task to carry it, and the walkthrough pages I would open in the morning show a stray ']' on most Inbox cards, 'needs you' badges on finished tasks, and rebase run pages that say nothing. It runs for an hour on its own; I would not yet leave it overnight.

## High

- **config** — The 'config change takes effect without a restart' goal and definition-of-done item was not delivered and has no task; the shipped operate skill and the Config page both say a restart is needed.
  - suggestion: File it as a phase 04 task (reload garden.yaml each tick when its mtime changes) or strike it from the goals with a sentence saying why.
- **merging** — 12 of 30 merges were done by hand (four hard-tier, eight when the queue rotated, and the CG-176 fix itself), so the queue that is supposed to let me leave the loop running has not been shown working live.
  - suggestion: Decide the hard-tier policy the operator asked about (merge after two approving rounds, or make the scratch-merge check the queue's own step), then run the queue on a batch of approved PRs before phase 04 opens many at once.

## Medium

- **inbox** — Eight of thirteen Inbox draft cards end in '· ] Tests cover ...' because the last-log-line helper returns the last checklist bullet of the body when the task has no log.
  - suggestion: Make _last_log_line read only the '## Log' section and return '' otherwise.
- **board and task page** — Done tasks still show a red 'needs you' badge on the Board (CG-164, CG-142) and 'automerge held: GitHub reports the PR unknown' on a merged task page (CG-125); CG-175 only cleans state on new transitions.
  - suggestion: Hide needs_human and automerge_blocked in every view when the task is terminal, and sweep existing state once on the next tick.
- **run page** — A mechanical rebase run page shows 'recorded a single claude-json result', '(no brief recorded)' and '(no final message)' twice, telling me nothing about what happened.
  - suggestion: Render a no-model rebase run as its own thing: base commit rebased onto, whether it was pushed, and the check results.

## Low

- **dispatch** — CG-176 went ready to failed because its worktree had uncommitted changes from an earlier run and needed a hand reset; the sidebar also shows a review running on CG-176 three minutes after it merged.
  - suggestion: Have dispatch reset or refuse a dirty worktree itself instead of failing the task, and cancel an in-flight review when its PR merges.
- **tick speed** — Ticks take about a minute because pre-merge checks and rebases run the full suite inside the tick, which also holds the web UI's lock.
  - suggestion: Prioritise CG-182 (checks and rebases as run records outside the tick) early in phase 04.

_garden persona run 20260905T101619Z-persona_
